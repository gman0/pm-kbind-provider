apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kbind-issuer
subjects:
  - kind: ServiceAccount
    name: kbind-issuer
    namespace: kbind
roleRef:
  kind: ClusterRole
  name: admin
  apiGroup: rbac.authorization.k8s.io
